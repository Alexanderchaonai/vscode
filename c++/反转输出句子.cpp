/*
小明同学写单词的时候喜欢反着写，比如 hello 他会写成 olleh。给出小明同学写的一个句子，请你将所有的单词复原。

输入格式
共一行，一个字符串表示句子，单词之间以空格分隔。

输出格式
每个单词一行。
例子：
输入：olleh dlrow
输出：hello world
*/
#include<iostream>
using namespace std;
int main()
{
    string s;
    while(cin>>s)
    {
        for(int i=s.size()-1;i>=0;i++)
        {
            cout<<s[i];
        }
        cout<<endl;
    }
    return 0;
}

/*这段代码的详细解释：
所以说cin>>a就是说在读取完一个单词就直接进行了里面的for循环，然后里面结束之后再进行下一次读取单词对吧
流程解释
读取单词：
每次执行 cin >> a 时，程序会从标准输入中读取下一个单词并存储在字符串 a 中。这会自动处理空格和换行符。


进入循环体：
读取完一个单词后，程序立即进入 while 循环的主体部分。


反转输出：
在循环体内，执行 for (int i = a.length() - 1; i >= 0; i--)，从字符串 a 的最后一个字符开始到第一个字符，依次输出字符，实现单词的反转。
cout << endl; 确保每个单词后换行。


准备下一次读取：
一旦当前单词处理完毕，程序会返回到 while (cin >> a) 这一行，准备读取下一个单词。
如果有下一个单词可读，程序重复上述步骤；如果没有更多单词（例如输入结束），循环就会停止。
总结
整个过程是一个读取-处理-读取的循环，保证了每个单词被独立处理，而不会累积在内存中。这种设计使得代码既高效又简洁。
*/

/*
对cin>>a输入流的详细解释说明：
cin >> a 会从标准输入流中读取下一个被空格、换行符等分隔的单词，并将其存储在字符串 a 中。这个操作会自动跳过前
导空格，并在遇到下一个空格、换行符或文件结束时停止。
*/


//这题不能先把整个字符串都录进去再反转，不然不会得到helloworld，而是得到world hello，因此我们需要在
//读取完一个单词之后立刻反转而不是等到所有单词录完之后再进行翻转